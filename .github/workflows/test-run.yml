name: 'Test Run'
on:
  - push

jobs:
  test_run:
    runs-on: ubuntu-latest
    steps:
      - uses: alehechka/action-create-branch@feature/output-create-status
        with:
          branch: test/output
